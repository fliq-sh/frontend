// Fliq design-system composites — opinionated building blocks layered on top of
// the shadcn primitives in components/ui. See docs/adr/0001 for the visual rules
// (monochrome + iridescent F-mark + traffic-light status).

export { SectionHeader } from "./SectionHeader";
export { StatusBadge, StatusDot } from "./StatusBadge";
export { StatCard } from "./StatCard";
export { EmptyState, type EmptyStateStep } from "./EmptyState";
export { TONE, jobStatusTone, type Tone } from "./tones";
